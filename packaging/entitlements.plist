<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- The bundled sd-server + libstable-diffusion.dylib are native third-party
       binaries loaded at runtime; these let the hardened runtime dlopen and run
       them (they are re-signed with the same Developer ID during packaging). -->
  <key>com.apple.security.cs.allow-jit</key>                 <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key> <true/>
  <key>com.apple.security.cs.disable-library-validation</key> <true/>
</dict>
</plist>
